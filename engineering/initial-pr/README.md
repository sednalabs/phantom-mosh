# Initial contribution: source-bound evidence

Engagement: `phantom-mosh-initial-record-20260907`.
Workflow: operator-supplied Remote-First Engineering v4.
Authorization: create an initial PR in `sednalabs/phantom-mosh`; no merge,
release, repository protection change, production switch or MoshWatch mutation.

The review generation is `decd9b705eb81626f694335b8d5940538beb06da`, tree
`5563894a83ff76fbc6fa2a6d1cf203712b06c5dd`. The engagement and selected-source
ledger are in `engagement.json`. This is a component/security-contract change,
not a complete audit of inherited Mosh or a terminal integration acceptance.

New files were developed as a local candidate overlay. An authenticated GitHub
connector supplied immutable source and publication operations; a complete local
checkout was unavailable during initial development. Local tests therefore
validate the new standalone component, NOT the entire inherited repository.
Hosted inherited CI remains the integration evidence source. Local test commands
and candidate blob identities are in `local-validation.json`; they must not be
silently carried across changed blobs. Earlier interrupted-workspace test receipts
are not reused as evidence for this reconstructed candidate.

The root upstream README is preserved by its exact existing blob at
`docs/upstream-mosh.md`. Existing runtime, license, packaging and CI source remains
unchanged. The only overwritten baseline file is the root README, now identifying
this fork and its implemented/unimplemented boundary. New implementation source
is original to this contribution; historical private research is summarized, not
copied into the public repository.

The PR discussion carries the final publication receipt: reconciled parent,
prepared/output tree and commit, branch readback, exact changed-path verification,
and current head/base/synthetic integration identities. Capturing output identity
outside its own commit avoids a self-referential evidence file.

`engineering/capture_generation.py` records what hosted CI actually checks out.
Run ID, attempt, source/tree/parents and workflow blob accompany test artifacts.
PR-head tests and synthetic-merge tests must not be conflated. No independent
review, statistical privacy result, native Windows client, SSH/PTY/SSP integration
or release acceptance is claimed by these engineering records.
